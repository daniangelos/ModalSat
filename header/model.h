#ifndef MODEL_H
#define MODEL_H

typedef struct model
{
	/* mundos
	 * relacao
	 * sequencia infinita de mundos
	 * em que as sentencas sao validas
	 */
} model_t;

model_t generate_model();


#endif /* MODEL_H */
